# Revisión manual requerida — javascript:S3776

**Archivo:** `/home/zamaer/Documentos/codigo-general/AI-DevSecOps-Control-Plane/workspace/uploads/4865d528-7c1e-431a-a8b4-065ce9fc8383/repo/src/dashboard/index.html:1675`  
**Razón:** El parche generado por la IA requiere revisión manual antes de aplicarse (no pasó la validación automática del parche).

## Propuesta de Ollama

```javascript
      function renderAiStatusBadge() {
        const dot = aiStatusBadge.querySelector(".dot");
        const model = aiStatus.model || "local model";
        
        updateDotColor(dot);
        updateAiStatusBadgeStyle(aiStatus.available);
        updateTextContent(aiStatus.available, model);
      }

      function updateDotColor(dot) {
        if (dot) {
          dot.style.background = aiStatus.available ? "var(--mint)" : "var(--t-mute)";
        }
      }

      function updateAiStatusBadgeStyle(isAvailable) {
        const colors = isAvailable
          ? ["var(--mint-soft)", "rgba(158,255,224,0.18)", "var(--mint)"]
          : ["var(--surface-2)", "var(--border)", "var(--t-dim)"];
        
        aiStatusBadge.style.background = colors[0];
        aiStatusBadge.style.borderColor = colors[1];
        aiStatusBadge.style.color = colors[2];
      }

      function updateTextContent(isAvailable, model) {
        const textToSet = isAvailable
          ? [t("local-model-ready"), `Live · ${model}`]
          : [t("ollama-offline"), "Offline · Ollama"];
        
        aiBadgeText.textContent = "";
        if (scanAdapterStatus) scanAdapterStatus.textContent = t(isAvailable ? "scanning-adapters" : "ai-adapter-offline");
        if (aiEngineSummary) aiEngineSummary.textContent = textToSet[0];
        if (aiModelChip) aiModelChip.textContent = textToSet[1];
      }
```
